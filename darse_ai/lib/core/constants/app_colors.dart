import 'package:flutter/material.dart';

class AppColors {
  // 🧠 Core AI Dark Theme
  static const background = Color(0xFF121214); // deep carbon black
  static const surface = Color(0xFF1A1A1D);     // elevated cards

  // ✨ Primary AI Accent
  static const primary = Color(0xFF00E676);     // neon emerald
  static const primaryGlow = Color(0xFF00FF88); // glow variant

  // 🧊 Text System
  static const text = Colors.white;
  static const mutedText = Colors.white70;
  static const faintText = Colors.white54;

  // ⚡ UI States
  static const success = Color(0xFF00E676);
  static const warning = Color(0xFFFFB300);
  static const error = Color(0xFFFF5252);

  // 🧱 Borders / Dividers
  static const border = Color(0x22FFFFFF);

  // 🤖 AI Chat Bubble Colors
  static const userBubble = Color(0xFF1F1F23);
  static const aiBubble = Color(0xFF151518);
}